const BASE_URL = 'https://norma.nomoreparties.space/api';

export const LOADING = BASE_URL + '/ingredients';

export const SENDING = BASE_URL + '/orders';

export const CHECK = BASE_URL + '/password-reset';

export const RESET = BASE_URL + '/password-reset/reset';

export const AUTHORIZATION = BASE_URL + '/auth/login';

export const REGISTRATION = BASE_URL + '/auth/register';

export const EXIT = BASE_URL + '/auth/logout';

export const REFRESH = BASE_URL + '/auth/token';

export const USER = BASE_URL + '/auth/user';
